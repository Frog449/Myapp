# 📱 คู่มือการติดตั้งแอป CaffeBook บนโทรศัพท์มือถือ

เอกสารนี้อธิบายวิธีนำแอปพลิเคชัน **CaffeBook** ไปติดตั้งและใช้งานบนโทรศัพท์มือถือ (รองรับทั้ง **Android** และ **iOS / iPhone**)

---

## 🎯 สิ่งที่ต้องเตรียมก่อนเริ่มใช้งานบนมือถือ

1. **เชื่อมต่อ Wi-Fi เดียวกัน:**
   - โทรศัพท์มือถือและเครื่องคอมพิวเตอร์ของคุณต้องเชื่อมต่อ **Wi-Fi วงเดียวกัน** หรือแชร์ Hotspot จากมือถือเข้าคอมพิวเตอร์
2. **เปิดเซิร์ฟเวอร์ Backend (PHP/MySQL):**
   - ดับเบิลคลิกเปิดไฟล์ `start_api.bat` บนเครื่องคอมพิวเตอร์
   - เซิร์ฟเวอร์จะเปิดรับการเชื่อมต่อจากภายนอกที่ `http://192.168.0.48:8000` (หรือดูหมายเลข IP ที่แสดงในหน้าต่างจอดำ)

---

## 🚀 วิธีที่ 1: ติดตั้งเป็นแอป Android (ไฟล์ .APK) — สำหรับ Android

### ขั้นตอนการสร้างไฟล์ APK:
1. ดับเบิลคลิกไฟล์ **`build_apk.bat`** หรือเปิด Terminal แล้วรันคำสั่ง:
   ```bash
   flutter build apk --release
   ```
2. เมื่อเสร็จสิ้น จะได้ไฟล์ติดตั้งอยู่ที่โฟลเดอร์:
   ```
   build\app\outputs\flutter-apk\app-release.apk
   ```

### ขั้นตอนการติดตั้งลงบนมือถือ:
1. **ส่งไฟล์ `app-release.apk` เข้ามือถือ** ด้วยวิธีใดวิธีหนึ่ง:
   - เสียบสาย USB ก๊อปปี้ไฟล์ลงในโฟลเดอร์ Download ของมือถือ
   - ส่งผ่าน LINE (Keep / แชทตัวเอง) หรือ Google Drive / Gmail
2. **กดติดตั้ง:**
   - บนมือถือ ให้เปิดแอป **"ไฟล์ (Files)"** หรือที่บันทึกไว้ แล้วแตะที่ไฟล์ `app-release.apk`
   - กดปุ่ม **"ติดตั้ง (Install)"**
   - *หมายเหตุ:* หากระบบขึ้นเตือนความปลอดภัย ให้เลือก **"อนุญาตการติดตั้งจากแหล่งที่ไม่รู้จัก (Allow from unknown sources)"**
3. **เปิดแอป CaffeBook:**
   - แอปจะปรากฏเป็นไอคอนบนหน้าจอมือถือของคุณทันที!

---

## 🌐 วิธีที่ 2: ติดตั้งผ่านเบราว์เซอร์มือถือ (PWA - Progressive Web App) — สำหรับ iPhone (iOS) และ Android

> **ข้อดี:** ไม่ต้องรัน Build APK ไม่ต้องใช้พื้นที่เยอะ ติดตั้งง่ายภายใน 5 วินาทีทั้งบน **iPhone** และ **Android**

### สำหรับ iPhone / iPad (Safari):
1. บน iPhone ให้เปิดแอป **Safari** แล้วพิมพ์ URL:
   ```
   http://192.168.0.48:8000
   ```
2. กดปุ่ม **แชร์ (Share)** (ไอคอนสี่เหลี่ยมลูกศรชี้ขึ้น ⬆️ ด้านล่างหน้าจอ)
3. เลื่อนลงมาแล้วเลือก **"เพิ่มไปยังหน้าจอโฮม (Add to Home Screen)"**
4. กด **"เพิ่ม (Add)"** ที่มุมขวาบน
5. จะมีไอคอนแอป **CaffeBook** ปรากฏบนหน้าจอ iPhone เหมือนแอปปกติ ใช้งานแบบเต็มหน้าจอ (Full Screen) ไม่มีแถบ URL

### สำหรับ Android (Google Chrome):
1. บน Android ให้เปิด **Google Chrome** แล้วพิมพ์ URL:
   ```
   http://192.168.0.48:8000
   ```
2. กดปุ่ม **เมนู 3 จุด (⋮)** ที่มุมขวาบน
3. เลือก **"ติดตั้งแอป (Install App)"** หรือ **"เพิ่มลงในหน้าจอหลัก (Add to Home screen)"**
4. กด **"ติดตั้ง (Install)"** แอปจะติดตั้งลงบนเครื่องทันที

---

## ⚙️ การตั้งค่า Server IP ในแอป (กรณีเปลี่ยน Wi-Fi หรือเปลี่ยน IP)

หากคุณนำมือถือไปต่อกับคอมพิวเตอร์เครื่องอื่น หรือหมายเลข IP เครื่องเปลี่ยนไป:
1. ในแอป CaffeBook ให้กดที่ไอคอน **Server Settings (รูปไอคอน Server / DNS 🌐)** บนแถบหัวข้อ (App Bar) หรือในเมนู
2. พิมพ์หมายเลข IP ใหม่ของคอมพิวเตอร์ เช่น `192.168.1.50:8000`
3. กดปุ่ม **"ทดสอบการเชื่อมต่อ (Ping Server)"**
4. เมื่อขึ้นเครื่องหมายสีเขียว ให้กด **"บันทึกและใช้งาน"** ระบบจะโหลดข้อมูลหนังสือใหม่ทันที!

---

## 🛠️ การแก้ไขปัญหาที่พบบ่อย (Troubleshooting)

| ปัญหา | สาเหตุ | วิธีแก้ไข |
|---|---|---|
| มือถือเปิดไม่ได้ / ขึ้นหมุนโหลดตลอด | มือถือกับคอมไม่ได้อยู่ Wi-Fi เดียวกัน | ตรวจสอบว่ามือถือและคอมต่อ Wi-Fi ชื่อเดียวกัน หรือเชื่อม Hotspot เดียวกัน |
| เชื่อมต่อ API ไม่ได้ | Windows Defender Firewall บล็อกพอร์ต 8000 | เข้า Windows Defender Firewall -> Allow an app through Firewall -> ตรวจสอบว่า PHP หรือ Apache ได้รับการติ๊กถูกทั้ง Private และ Public |
| หมายเลข IP เปลี่ยน | เราเตอร์แจก IP ใหม่เมื่อต่อ Wi-Fi ใหม่ | เปิด `start_api.bat` เพื่อดูหมายเลข IP ล่าสุด แล้วนำไปใส่ในเมนูตั้งค่าของแอป |
